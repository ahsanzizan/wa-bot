// @ts-check

import eslint from "eslint";
// @ts-expect-error: The typescript-eslint doesn't exist (idk how to fix that yet)
import tseslint from "typescript-eslint";

export default tseslint.config();